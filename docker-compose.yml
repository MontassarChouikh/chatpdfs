version: '3'

services:
  flask-app:
    build: .
    container_name: flask1
    ports:
      - "5001:5001"
    volumes:
      - .:/app

    environment:
      - FLASK_APP=app.api  # Reference the api.py file where the app object is defined



      